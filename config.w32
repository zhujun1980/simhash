// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("simhash", "for simhash support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("simhash", "enable simhash support", "no");

if (PHP_SIMHASH != "no") {
	EXTENSION("simhash", "simhash.c");
}

